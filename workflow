0. Working with tasks
    a. Get task notification from ClickUp app
    b. krijojme nje branch te ri tek VS Code
    c. fillojme ndyshimet
    d. testojme
    e. bejme commit+push ne branch
    f. ndyshojme statusin e taskut ne ClickUp 


1. Redefine CSS
    a. vendosim border rreth elementit qe duam te stilojme
    b. krahasojme stilin e elementit me templatin
    c. fillojme remodelimin nga containers

    d. width -> %       (pervec elementeve te pozicionuar si absolute[vw,vh,px])
    e. height -> nuk do percaktojme (kur na duhet te percaktojme height-> min-height: 100vh)

    f. spacing ->   margin(jasht border) padding (brenda border) 
                    percatojme me njesine "em" -> padding: 1em;
                    
                    .button{
                        font-size: 20px;
                        padding: 1em;
                    }
                    
                    1em = 20px || 0.5em = 10px

    g. positioning  -> container -> flex || justify -> left-right  // align -> top-bottom
                                            flex-direction: column // flex-wrap: wrap; -> lejon elementet
                                                                       e flex container qe te kalojne ne rreshtin tjt
                    -> position absolute -> top: 10px; left: 10px;

    h. font-size: 1rem = 16px

2. Redefining colors
    a. copy colors from templates
    b. percaktojme ngjyrat duke perdorur CSS variables

3. Adding products data @ db.json

4. Refactoring JS
